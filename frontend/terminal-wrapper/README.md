# Terminal embed wrapper

Static page used as the Miro embed URL once `WRAPPER_URL` is set in
`frontend/src/backendConfig.ts`. See the root `README.md`'s
"Turn on the shared wrapper" and "Security" sections for how it works and how
to deploy it — this folder is just the page itself (`index.html`), unchanged
from `miro-ide`'s version.
