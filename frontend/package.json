{
  "name": "miro-terminal-frontend",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "lint": "tsc --noEmit",
    "pages:publish": "npm run pages:publish:app && npm run pages:publish:wrapper",
    "pages:publish:app": "npm run build && gh-pages -d dist -e app --add",
    "pages:publish:wrapper": "gh-pages -d terminal-wrapper -e terminal-wrapper --add"
  },
  "devDependencies": {
    "@mirohq/websdk-types": "latest",
    "gh-pages": "^6.1.1",
    "typescript": "^5.4.5",
    "vite": "^5.2.0"
  }
}
